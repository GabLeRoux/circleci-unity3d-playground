# .circleci/config.yml

version: 2.0
jobs:
  build:
    docker:
      - image: gableroux/unity3d:2019.3.0f6-android
    steps:
      - checkout
      - run:
          name: hello world
          command: |
            echo 'hello world'
      - run:
          name: Check java version
          command: java -version
